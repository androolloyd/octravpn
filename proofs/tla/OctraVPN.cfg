SPECIFICATION Spec

CONSTANTS
    Validators        = {v1, v2, v3}
    Clients           = {c1}
    MinBond           = 2
    MinDeposit        = 1
    MaxSeq            = 3
    SlashBountyBps    = 1000
    SlashBurnBps      = 5000
    SlashTreasuryBps  = 4000

INVARIANTS
    Invariants

PROPERTIES
    Liveness_SettleOrRefund

CONSTRAINT
    StateConstraint

\* Bound the explored state space so model checking terminates.
\* Defined inline below.
SYMMETRY Symmetry

\* TLC operator overrides

\* StateConstraint defined here keeps us within ~10k states for fast checks.
\* (See accompanying README in this directory for how to scale up.)

# cargo-audit / cargo-deny configuration.
# Run: cargo install --locked cargo-audit cargo-deny
#      cargo audit
#      cargo deny check
[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
ignore = []
informational_warnings = ["unmaintained", "unsound"]
